{
  "name": "krypt-microservices",
  "version": "1.0.0",
  "description": "Orchestrates all Krypt microservices",
  "private": true,
  "scripts": {
    "docker:up": "docker-compose up --build",
    "docker:down": "docker-compose down",
    "docker:clean": "docker-compose down -v --rmi local"
  }
}
